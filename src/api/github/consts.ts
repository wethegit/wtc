/** Company GitHub organization used for template discovery and new repos. */
export const GITHUB_REPO_OWNER = "wethegit";

/** Team slug whose numeric ID is resolved when creating repo ruleset bypasses. */
export const GITHUB_RULES_BYPASS_TEAM_SLUG = "senior-reviewers";
